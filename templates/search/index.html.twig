{% extends 'frontend/base.html.twig' %}

{% block title 'Listing All Properties' %}

{% block body %}

    {# <h2 class="mt-4 mb-4 text-center">List of properties</h2>

    {{ form_start(form) }}
        
    <button class="btn btn-primary" type="submit">Rechercher</button>
    {{ form_end(form) }} #}

    {# <div class="count">
        There is {{ pagination.getTotalItemCount }} properties in the database
    </div> #}

    {# <div class="row">
        {% include 'property/_card.html.twig' %}
    </div>

    <div class="navigation">
        {{ knp_pagination_render(properties) }}
    </div> #}


    <div class="container js-filter">
        <div class="row">
    		<div class="col-md-3">
    			{% include 'search/_filter.html.twig' %}
    		</div>
    		<div class="col-md-9">
                
    			<div class="d-flex justify-content-end mb-2 js-filter-sorting">
    				{% include 'search/_sorting.html.twig' %}
    			</div>
		    	<div class="js-filter-content">
		    		{% include "search/_properties.html.twig" %}
		    	</div>
                <div class="navigation mt-4 js-filter-pagination">
					{% include "search/_pagination.html.twig" %}
                </div>
    		</div> 
    	</div>
    </div>

{% endblock %}