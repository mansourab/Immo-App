{% extends 'frontend/base.html.twig' %}

{% block body %}
    <h1>Edit Owner</h1>

    {{ include('owner/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_owner_index') }}">back to list</a>

    {{ include('owner/_delete_form.html.twig') }}
{% endblock %}
