{% extends 'backend/admin/base.html.twig' %}

{% block title 'Create new Category' %}

{% block body %}

{#     <h2 class="mt-4 mb-4 text-center">Create new category</h2>

    {{ form_start(category_create_form) }}
    {{ form_widget(category_create_form) }}
        <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(category_create_form) }} #}

    <div class="wrapper dashboard-wrapper">
        <div class="d-flex flex-wrap flex-xl-nowrap">
            {# Sidebar #}
                {% include 'backend/admin/sidebar.html.twig' %}
            {# EndSidebar #}

            <div class="page-content">
                    
                {# Header #}
                    {% include 'backend/admin/layouts/header.html.twig' %}
                {# EndHeader #}

                <main id="content" class="bg-gray-01">
                    <div class="px-3 px-lg-6 px-xxl-13 py-5 py-lg-10">
                        <div class="tab-content shadow-none p-0">
                            <div class="card-body py-4 py-md-0 px-0">
                                <div class="row">
                                    <div class="col-lg-6">
                                        <div class="card mb-6">
                                            <div class="card-body p-6">
                                                <h3 class="card-title mb-0 text-heading fs-22 lh-15">
                                                    Ajouter une nouvelle catégorie
                                                </h3>                                                
                                                {{ form_start(category_create_form) }}
                                                {{ form_row(category_create_form.name, {attr: {'class': 'form-control-lg border-0'}}) }}
                                                    <button type="submit" class="btn btn-indigo">Enregistrer</button>
                                                {{ form_end(category_create_form) }}
                                            </div>
                                        </div>
                                    </div>                                       
                                </div>
                            </div>
                        </div>
                    </div>
                </main>
            </div>
        </div>
    </div>


{% endblock %}